import React from "react";
import {Icon} from "src/shared/ui/icons/Icon";

const AddToCart: React.FC = () => {
  return (
    <div>
      <Icon icon="addToCart" iconStyle={"w-[32px] h-[32px]"} />
    </div>
  );
};

export default AddToCart;
